import SHOP_DATA from "../../data/shop.data";

const categoriesReducer = (state = SHOP_DATA, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default categoriesReducer;
